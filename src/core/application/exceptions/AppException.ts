/**
 * An exception that is thrown by the application.
 *
 * An exception is a type of error that is expected to be caught and handled.
 * It is used to signal that something expected happened.
 * It is not used to signal that something unexpected happened.
 * @see AppError
 */
export class AppException extends Error {}
